steps:

- name: 'list-files'
  script: ls -l

- name: 'Build'
  script: |
    #!/usr/bin/env bash
    echo "Build Started"
    echo "Build done"

- name: 'Test'
  script: echo "Tests running"
  
- name: 'Deployment'
  script: |
    #!/usr/bin/env bash
    echo "Deployment Started"
    echo "Deployment done"

logsBucket: 'gs://gds-log-bucket'
options:
  logging: GCS_ONLY
